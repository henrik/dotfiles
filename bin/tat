#!/bin/sh
#
# Attach or create tmux session with the provided name, or else named
# the same as the current directory. These have the same effect:
#
#   tat some-project
#
#   cd some-project
#   tat
#
# Based on https://github.com/thoughtbot/dotfiles/blob/master/bin/tat
#

session_name=`basename ${1:-$PWD}`
tmux new-session -As "$session_name"
